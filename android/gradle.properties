org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Djava.net.preferIPv4Stack=true -Djava.net.preferIPv6Addresses=false
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Run Gradle in a single JVM (no daemon) and disable VFS file-watching so the
# build doesn't need a loopback socket — works around sandboxed-network builds.
org.gradle.daemon=false
org.gradle.vfs.watch=false
